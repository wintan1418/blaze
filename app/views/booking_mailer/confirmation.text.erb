BLAZE CAFE — Booking Confirmed
==============================

Ref: <%= @booking.reference %>

For: <% if @bookable.is_a?(GamingSlot) %><%= @bookable.gaming_console.label %><% elsif @bookable.is_a?(Screening) %><%= @bookable.title %><% end %>
When: <%= @bookable.starts_at.strftime("%A, %B %d · %I:%M %p") %>
Seats: <%= @booking.seats %>
Total: ₦<%= (@booking.total_price_kobo / 100).to_s.reverse.scan(/\d{1,3}/).join(",").reverse %>

Show this email (or your reference) at the counter.

Blaze Cafe · Ondo State, Nigeria
Where good times come alive.
